#!/usr/bin/env python3
"""
Excel Table Finder CLI entry point.

This script serves as the command-line entry point for the excel-table-finder package.
It can be used when installing via pip with the --editable flag or traditional installs.
"""

import sys
from src.cli import main

if __name__ == "__main__":
    sys.exit(main())
